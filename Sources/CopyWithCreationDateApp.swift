import SwiftUI

@main
struct CopyWithCreationDateApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 720, minHeight: 500)
        }
        .windowStyle(.titleBar)
    }
}
